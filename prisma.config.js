import { defineConfig } from '@prisma/config';

export default defineConfig({
  datasource: {
    url: process.env.postgresql://postgres.kdwboitvmmgedwpxhtcn:20B311%40Cl%40ra24@aws-0-eu-central-1.pooler.supabase.com:6543/postgres,
  },
});
